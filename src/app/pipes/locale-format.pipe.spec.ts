import { LocaleFormatPipe } from './locale-format.pipe';

describe('LocaleFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new LocaleFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
